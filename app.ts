// Define a function that takes two numbers and returns their sum
function addNumbers(a: number, b: number): number {
    return a + b;
  }
  
  // Call the function
  const result = addNumbers(6, 3);
  console.log(result); // Output: 8